'use strict'

// Shared type constants (runtime enums) for LLM providers

const PROVIDERS = Object.freeze({ ANTHROPIC: 'anthropic', GEMINI: 'gemini', OPENAI: 'openai' })

module.exports = { PROVIDERS }
